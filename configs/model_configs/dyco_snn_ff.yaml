name: dyco_snn
T: 16                   # raised from 8 to fit the 6-layer (2-block) cascade; must match data config
num_classes: 2
image_size: 32
in_channels: 2

mode: ff                # Param-matched feedforward LIF stack baseline.

n_blocks: 2
stem_dim: 64
n_l5: 48

# FF-block-specific (tune ff_hidden_mult to match D+C param count).
ff_hidden_mult: 2.0
ff_depth: 2

# The following are ignored in ff mode but kept for config compatibility.
learnable_tau: false
learnable_asc: false
heterogeneous_init: false
recurrent_enabled: false
alpha_init: 0.0
alpha_learnable: false

# Spike threshold (shared across all D/C/DC/FF arms for a fair ablation).
# 0.5 (not 1.0): at v_th=1.0 each GLIF3 layer needs more steps to cross threshold,
# leaving the deep 2-block cascade subthreshold; 0.5 speeds per-layer charge-up
# (paired with T=16) without touching tau_m.
v_th: 0.5
